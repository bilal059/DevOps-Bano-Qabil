name: Run Script
on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run run.sh
        run: ./run.sh
